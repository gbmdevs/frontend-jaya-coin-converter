name: CI/CD for Frontend (React + Vite) - Jaya Coin Converter

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    enviroment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Build the project
        env:
          VITE_API_URL_BACK_END: ${{ secrets.VITE_API_URL_BACK_END }}
        run: npm run build

      - name: Install Heroku CLI
        run: |
          curl https://cli-assets.heroku.com/install.sh | sh
          heroku --version

      - name: Deploy to Heroku
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
          HEROKU_DEBUG: 1
          HEROKU_APP_NAME: frontend-jaya-coin-converter
        run: |
          echo "Logging in to Heroku..."
          export HEROKU_API_KEY="$HEROKU_API_KEY"
          echo "HEROKU_API_KEY is set to: ${HEROKU_API_KEY:0:10}..."

          echo "Clearing existing buildpacks..."
          heroku buildpacks:clear -a $HEROKU_APP_NAME

          echo "Setting heroku/nodejs buildpack..."
          heroku buildpacks:add heroku/nodejs -a $HEROKU_APP_NAME

          echo "Deploying to Heroku app: $HEROKU_APP_NAME"
          git remote add heroku https://git.heroku.com/${HEROKU_APP_NAME}.git || git remote set-url heroku https://git.heroku.com/${HEROKU_APP_NAME}.git
          git push heroku develop:main --force